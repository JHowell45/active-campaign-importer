# active-campaign-importer
Grab all of your data from Active Campaign into local storage and convert it into spreadsheets.


### How to run the importer:
the command is:
```bash
docker-compose run --rm importer ...
```